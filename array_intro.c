#include<stdio.h>

int main(){
    //An array of 5 numbers
    int number[] = {10, 20, 30, 40, 50};

    // Way to view the first value of the array (index always starts at 0)
    printf("First number is: %d\n", number[0]);
    printf("Third number is: %d\n", number[2]);
    
    return 0;
}